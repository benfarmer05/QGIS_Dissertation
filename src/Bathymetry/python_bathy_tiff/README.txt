This directory will become more organized, and designed so that it is more so a destination for scripts than the working directory. the working directory for all scripts should be either my laptop’s home directory, or Box home directory (probably the latter). A git-based version control flow will be established as well. This directory here is merely a backup, which itself will be mirrored